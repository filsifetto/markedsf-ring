% !TEX root = ../main.tex
% Agent documentation:
% Purpose: container chapter for market actions, pilot design, scaling plan, and
% control system.
% Downstream use: feeds the financial analysis and operational decision points.
% Revision guardrails: keep the order as tactics -> Nesodden launch ->
% long-term plan -> control, because each subsection depends on the previous
% one.
\section{Aktivitetsprogram}

Aktivitetsprogrammet viser hvordan Zipline bør gå fra første lansering på
Nesodden til kommersiell etablering i Bærum. Asker kan legges til som
replisering innen samme primærsegment hvis Bærum viser gode nok resultater.
Logikken følger segmenteringen. Nesodden brukes først fordi området har et
tydelig tilgjengelighetsproblem og gir et kontrollert læringsmarked. Bærum
kommer etterpå fordi markedet tester om Zipline kan vinne kunder fra Foodora
og Wolt i primærsegmentet.

Kapittelet er bygd opp i fire deler. Tiltakene etablerer den taktiske
rammen rundt posisjonering og kjøpsbarrierer. Pilotbeskrivelsen viser
hvordan Nesodden skal brukes som første fase. Den langsiktige planen
skisserer 24-måneders ekspansjonen, og kontrollkapittelet beskriver
hvordan fasene følges opp og justeres underveis.

Figur~\ref{fig:gantt} viser tidslinjen for hele aktivitetsprogrammet,
med faseinndeling, tverrgående arbeid og kontrollpunktene.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    x=0.62cm, y=0.65cm,
    every node/.style={font=\footnotesize},
    milestone/.style={diamond, draw, fill=highred,
                      minimum size=5pt, inner sep=0pt}
]
% Background grid every 3 months
\foreach \m in {0,3,6,9,12,15,18,21,24} {
    \draw[gray!25, dashed, thin] (\m,0) -- (\m,5);
}

% Time axis
\draw[->, thick, gray!60] (0,0) -- (25.5,0);
\foreach \m in {0,3,6,9,12,15,18,21,24} {
    \node[anchor=north, font=\scriptsize] at (\m,-0.05) {\m};
}
\node[anchor=north, font=\small] at (12,-0.6) {Måned};

% Phases (top-down)
\draw[fill=blue!35, draw=blue!60, rounded corners=1pt]
    (0,3.5) rectangle (12,4.05);
\node[anchor=west] at (0.2,3.78) {Fase 1: Nesodden};

\draw[fill=orange!50, draw=orange!70, rounded corners=1pt]
    (6,2.7) rectangle (18,3.25);
\node[anchor=west] at (6.2,2.98) {Fase 2: Bærum og Asker};

\draw[fill=green!40, draw=green!60, rounded corners=1pt]
    (18,1.9) rectangle (24,2.45);
\node[anchor=west] at (18.2,2.18) {Fase 3: Konsolidering};

% Cross-cutting work
\draw[fill=gray!25, draw=gray!50, rounded corners=1pt]
    (0,1.1) rectangle (24,1.65);
\node[anchor=west] at (0.2,1.38) {Regulatorisk arbeid};

\draw[fill=gray!25, draw=gray!50, rounded corners=1pt]
    (0,0.4) rectangle (24,0.95);
\node[anchor=west] at (0.2,0.68) {Partnerarbeid};

% Milestones at top
\foreach \m/\lbl in {3/{MV}, 6/{SV/BP}, 12/{BP/PM}, 18/{BP}, 24/{BP}} {
    \node[milestone] at (\m,4.7) {};
    \node[anchor=south, font=\scriptsize, highred] at (\m,4.85) {\lbl};
}
\end{tikzpicture}
\caption[Tidslinje for aktivitetsprogrammet]{Tidslinje for
aktivitetsprogrammet over 24 måneder. Fase 1 og fase 2 overlapper i
måned 6--12. MV = mellomvurdering, SV = skaleringsvurdering, BP =
beslutningspunkt, PM = posisjonsmåling.}
\label{fig:gantt}
\end{figure}

\import{./Sections/}{07a_Tiltak}

\import{./Sections/}{07b_Pilot_Nesodden}

\import{./Sections/}{07c_Langsiktig_plan}

\import{./Sections/}{07d_Oppfolging_og_kontroll}
